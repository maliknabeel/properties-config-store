server:
  port: 8000

eureka:
  client:
    service-url:
      defaultZone: http://eureka:pafiast@localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true